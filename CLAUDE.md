# CLAUDE.md — LeRaffl BEV Trajectory Gallery

This repository's agent instructions are kept **tool-agnostic** in a single
source of truth, `AGENTS.md`, so that every assistant — Claude and non-Anthropic
tools alike — follows the same guidance and the two files can never drift apart.

Read and follow it in full:

@AGENTS.md

It has two parts: **Part 1** for answering questions about the data (where the
numbers, sources and definitions live), and **Part 2** for changing the repo
(architecture map, invariants, the "when you change X, also update Y" checklist,
and conventions). Start with whichever fits the task in front of you.
